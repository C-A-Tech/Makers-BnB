<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Makers BnB</title>
    <link rel="stylesheet" href="/css/home.css" />
  </head>
  <body>
      <div class="navbar">
        <img src="/images/logo.png" alt="bobross" class="logo" />
        <ul>
          <li><a href="/home">Home</a></li>
          <li><a href="/space/new">List a Space</a></li>
          <li><a href="#">Search</a></li>
          <li><a href="#">Profile</a></li>
        </ul>
      </div>
      <h1>Spaces</h1>
        <ul>
          <% @spaces.each do |space|%> <% if space.available == 't' %>
          <div class="container">
            <li>
              <form action="/booking" method="post">
                <article>
                  <p>
                    <%= space.first_name.capitalize %> <%=
                    space.last_name.capitalize %> posted:
                  </p>
                  <h2><%= space.title %></h2>
                  <p><strong>£<%= space.price %> per night</strong></p>
                  <p><%= space.description %></p>
                  <p>
                    Available from <%= space.available_from %> until <%=
                    space.available_to %>
                  </p>
                  <img src='images/new_space_uploads/<%= space.image %>' >
                  <input
                    type="hidden"
                    name="title"
                    value="<%= space.title %>"
                  />
                  <input
                    type="hidden"
                    name="description"
                    value="<%= space.description %>"
                  />
                  <input
                    type="hidden"
                    name="price"
                    value="<%= space.price %>"
                  />
                  <input
                    type="hidden"
                    name="available_from"
                    value="<%= space.available_from %>"
                  />
                  <input
                    type="hidden"
                    name="available_to"
                    value="<%= space.available_to %>"
                  />
                  <input
                    type="hidden"
                    name="space_id"
                    value="<%= space.id %>"
                  />
                  <input class="book" type="submit" value="Book" />
                </article>
              </form>
            </li>
          </div>
          <% end %> <% end %>
        </ul>
    </div>
  </body>
</html>
